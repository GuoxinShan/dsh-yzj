# 830 真数据闭环实验（转交执行版）

> 日期：2026-08-19 ｜ 设计：Guoxin Shan / agent ｜ 执行：转交他人（人或 agent 均可）
> 目的：用「830 项目【登顶计划】」群的**真实历史数据**验证 AI推进闭环——知识沉淀 → 立项回放 → 真巡检增量发现，产出**打扰判据精度的第一份真机证据**（对应 `docs/migration/advance-lingee-migration.md` §4 验证清单第 1/2 行）。
> 实验结束后：证据写入 `docs/status/gap-analysis.md` §24.6（模板见本文 §7），截图存 `.acceptance/shots-advance-830/`。

## 0. 判定总表（最后对照这张表给结论）

| # | 判定项 | PASS 条件 |
|---|---|---|
| 1 | 知识底座 | 知识库出现 ≥3 篇四段式纪要（目标/内容/共识/下一步），各有 docId |
| 2 | 立项 | 看板出现事项「830：从参谋部到 AI推进」，立项经**一张**确认卡 |
| 3 | 历史回放 | 时间旅程 ≥4 行；其中 8/12 定义卡那条是「目标更新」且**弹了确认卡**（全程第二张、也是最后一张卡） |
| 4 | 真巡检发现 | 看板队列头变为「上次巡检 HH:mm · 本轮发现 N 条」且 **N ≥ 1** |
| 5 | 信号命中 | 农佳捷三点需求（8/18 16:39）和/或冯胜龙 PRD 消息被 feed 进事项（refs 含真实 msgId） |
| 6 | 噪音拒绝 | 廖衍清的竞争力报告讨论**没有**被喂进本事项 |
| 7 | 过滤自身 | 单国鑫本人 8/19 16:51 发的 6 条交付消息不出现在 scan 信号里 |
| 8 | 幂等 | 让模型对同一 msgId 再 feed 一次 → 返回幂等、事元不加行 |
| 9 | 卡片总数 | 全程确认卡**恰好 2 张**（立项 + 目标更新）；多一张即 FAIL 并记录是哪次写触发的 |

## 1. 环境与前置检查

- GUI：tmux 会话 `dsh-web`，`http://127.0.0.1:3080/`。重启方式：`tmux send-keys -t dsh-web C-c`，等 3080 关闭后 `tmux send-keys -t dsh-web 'cd /Users/guoxinshan/dev/deepseek-harness && node apps/cli/lib/bin.js web --port 3080' C-m`。**不要杀 `--profile ops` 的进程**。
- `yzj-cli` 已登录「单国鑫」（`openId=64a7e43ae4b07742af0af59d`）。scan 会过滤本人与 `BOT-` 消息。
- 看板/待办/测试会话已于 2026-08-19 下午清空；`~/.dsh/storages/yzj_advance_scan_cursors.json` 已重置为空。若发现残留探针，先按 gap §24.5 之前的清理方式处理再开始。
- root Chat 模型选 **DeepSeek-V4-Pro**（2026-08-19 当天 Grok 4.6 High 持续 Connection error）。
- 浏览器自动化注意（沿 `.acceptance` 既有脚本经验）：官方 composer placeholder 是「描述你想要构建的内容」/「给智能体发消息」，发送按钮 aria-label「发送消息」；`textarea:enabled` 的 last() 会误命中隐藏的「发到 群」输入框。
- 每次改 host/浏览器产物后要重启 GUI 才生效；本实验**不改代码**，只需实验中途改 cursor 时重启一次（§5）。

## 2. 材料清单（ID 已核实）

| 材料 | ID / 位置 |
|---|---|
| 群「830 项目【登顶计划】」 | groupId `6a605c7ce4b0772a6279295e` |
| 农佳捷三点需求（8/18 16:39，关键信号） | msgId `6a841a46e4b0ab2392c31eb9` |
| 冯胜龙《AI推进-产品PRD v2.1.md》消息（8/18） | msgId `6a842792e4b08c3f7ebf8521`，fileId `6a84279269855600019ba7ba` |
| 农佳捷原型 HTML（8/17） | fileId `6a83f1947f37950001692878` |
| 群文件里的历史转录（第 0 波原料，fileId 现场用 `yzj-cli im message list` / 群文件查） | `0812 AI 参谋部日例会.txt`、`转录：智能任务推进系统架构讨论 2026年8月12日1551.txt`、`转录：AI推进任务管理功能方案讨论 2026年8月13日1634.txt`、《智能纪要：AI参谋产品方案讨论 2026年8月6日.docx》 |
| 本地最新会议转录（0819 14:00） | `/Users/guoxinshan/Downloads/转录：AI推进产品方案讨论 20260819 1400.txt` |
| 知识库落点（默认，未另行指定时） | 个人「我的知识」 KB_ID `6a7042a26d7c9d1692e60593` |
| cursor 文件 | `~/.dsh/storages/yzj_advance_scan_cursors.json`（GUI 进程内存是权威，改文件必须先停 GUI） |

## 3. 第 0 波 · 知识底座（转录 → 标准纪要 → 知识库）

1. 在「我的知识」建父文档：`yzj-cli doc create --workspace 6a7042a26d7c9d1692e60593 --title "830实验·共识"` → 记 `PARENT_ID`。
2. 逐份转录产出**金蝶标准纪要**（严格四段：目标 / 内容 / 共识 / 下一步；docx 先 `doc download-url` 下载解析）。至少做三份：0812 日例会、0813 方案会、0819 产品会（本地文件）。
3. 每篇 `yzj-cli doc import --workspace 6a7042a26d7c9d1692e60593 --items '[{"fileName":"<纪要名>.md","content":"..."}]'`，再 `doc move` 挂到 `PARENT_ID` 下；**记录每篇 docId**（第 1 波 feed 的 refs 要用）。
   - 注意 import 的 content 不得以与 fileName 相同的一级标题开头（标题会重复）。
4. 红线：只写个人「我的知识」，**不动企业知识库**。

## 4. 第 1 波 · 立项 + 历史回放（GUI root Chat 操作）

打开 3080 → 新会话（root Chat，非群房间）→ 按顺序发以下提示词，每次等模型完成：

**① 立项**（预期弹确认卡 → 人点确认）：

> 用 yzj_advance_create 立项：title「830：从参谋部到 AI推进」；background「项目始于 AI参谋部/幕僚长构想（8/6 方案讨论、8/10 参谋场景），要把战略讨论变成可执行旅程」；goal「跑通 听会→标准纪要→共识入知识库→下一步入任务 的最小回路，产品口径对齐 PRD v2.1」；metrics 三行「金蝶标准纪要: 0 / 有」「共识入知识库: 0 / 有可溯源文档」「下一步可挂推进时间线: 0 / 有」；targetDate 2026/08/31；tags #830。

**② 回放 8/6–8/11 参谋部阶段**（纯追加，预期**不**弹卡）：

> 对「830：从参谋部到 AI推进」feed 一条进度更新：sourceType=文档，summary「参谋部阶段共识：幕僚长价值=真实理解×合法授权×闭环执行×时间连续性」，refs 带第 0 波 0806/0812 纪要的 docId。

**③ 回放 8/12 定义转向**（改基准，预期弹**全程第二张也是最后一张**确认卡）：

> 8/12 产品定义卡把方向从 AI参谋部改为 AI推进。请 feed：changeType=目标更新，goal 改为「按 AI推进产品定义：六态看板+最小推进回路，跑通纪要→共识入库→下一步编排」，summary 说明转向，refs 带 0812 纪要 docId。

**④ 回放 8/13 方案会**（纯追加，不弹卡）：

> feed 进度更新：8/13 推进任务管理功能方案讨论完成用户旅程对齐，refs 带 0813 纪要 docId。

验收：切「推进」页签 → 点开事项 → 时间旅程 ≥4 行（立项 + 3 条回放），目标区显示新 goal，指标卡三行可见。

## 5. 第 2 波 · cursor 回拨 + 真巡检（核心验证）

1. **停 GUI**（§1 命令；确认 3080 无监听）。
2. 取回拨锚点（农佳捷需求**前一条**消息）：
   `yzj-cli im message list --group-id 6a605c7ce4b0772a6279295e --msg-id 6a841a46e4b0ab2392c31eb9 --type old --limit 1` → 记返回的 msgId 为 `ANCHOR`。
3. 写 cursor 文件（整文件覆盖）：

```json
{
  "unit": { "name": "yzj_advance_scan_cursors", "version": 0 },
  "global": null,
  "tables": {
    "cursors": {
      "6a605c7ce4b0772a6279295e": {
        "lastMsgId": "<ANCHOR>",
        "scannedAt": 1787000000000,
        "groupName": "830 项目【登顶计划】"
      }
    },
    "meta": {}
  }
}
```

4. **重启 GUI**，等 3080 可访问。
5. root Chat 新会话（DeepSeek-V4-Pro）发：

> 请调用 yzj_advance_scan，groups=["830 项目【登顶计划】"]，limit=20。然后按巡检纪律处理：把新信号交给 yzj_advance_inspect，与在途事项比对；进度正常就静默 feed（refs 用真实 msgId），命中打扰判据才 stageTo=decision-needed；无关信号不要写。

   - **prompt 里不要写「你会发现 N 条」之类的暗示**（避免假阳性）；判定只看工具卡与看板。
   - 8/18 16:39 之后的群消息可能超过 20 条：若模型报告 cursor 已前进但还有增量，允许它**再 scan 一轮**（同一会话再发一句「再 scan 一次同一群」），两轮内应覆盖到 PRD 消息。
6. 对照 §0 判定 4–7。然后测幂等（判定 8）：

> 对刚才已喂过的那条 msgId 再 feed 一次同样内容。

   预期模型返回幂等/跳过，`yzj_advance_get` 读事元数不变。

## 6. 预期信号与噪音（执行者对照用）

回拨点之后的群消息构成（截至 2026-08-19 16:51）：

- **信号**：农佳捷三点需求（标准纪要/共识入库/下一步编排，明说②③是 AI推进需求）；冯胜龙 PRD v2.1 文件消息；「明天评审一下」；8/19 上午关于 PRD 的讨论。
- **噪音**：廖衍清等的战略竞争力报告评测线（与本事项无关，不应入）。
- **必被过滤**：单国鑫本人消息（含 16:51 的 1 条文本 + 5 条文件交付）、任何 `BOT-` 消息。

合理的模型行为区间：把农佳捷需求/PRD 作为进度更新静默挂上（refs=msgId）即 PASS；若模型判断需求文本构成指标口径变化而走 decision-needed 也算 PASS（记录判据编号）；把竞争力报告喂进来记 FAIL（判定 6）。

## 7. 证据归档模板（gap §24.6）

```
## 24.6 AI推进｜830 真数据闭环实验（2026-08-XX，执行人：XXX）
环境：GUI 3080 重启于 HH:mm；cursor 回拨至 <ANCHOR>（农佳捷需求前一条）。
第 0 波：纪要 N 篇入「我的知识/830实验·共识」（docId 列表…）。
第 1 波：A-YYYYMMDD-NNN 立项 + 3 条回放；确认卡 2 张（立项/目标更新）。
第 2 波：scan 发现 N 条（截图 shots-advance-830/…）；命中信号=…；噪音拒绝=…；幂等=…。
判定总表：9 项 PASS/FAIL 各项结果；FAIL 项附现象与工具卡截图。
结论：打扰判据精度证据（哪些静默、哪些进队列、是否符合 §13 判据）。
```

## 8. 红线

1. **不往 830 群发任何消息**（实验全程只读群；知识库写入只进「830实验·共识」）。
2. 不动企业知识库、不删群文件。
3. 不杀 `--profile ops` daemon；重启只针对 3080 web GUI。
4. 全程确认卡应恰好 2 张；出现第 3 张先记录再决定是否放行（大概率是模型误带基准字段——拒绝并让它纯追加重试）。
5. 实验产物集中在「830实验·共识」目录与一条推进事项内，便于事后一次清理；**不要**清理群里的真实历史。

## 9. 实验完成后的后续工作（执行者义务 + 交回件）

### 9.1 无论结果如何（必做）

1. **证据归档**：按 §7 模板写入 `docs/status/gap-analysis.md` §24.6；截图入 `.acceptance/shots-advance-830/`（该目录会被 git 跟踪，`shots/` 才是忽略的）。
2. **勾验证清单**：更新 `docs/migration/advance-lingee-migration.md` §4 表格——「打扰判据精度」与「事元 schema 表达力」两行的证据现状栏，填 §24.6 指针与一句话结论（PASS 写结论，FAIL 写现象）。
3. **坑回写**：排查中若出现「现象与文档预期不符」「超过一次构建-验证循环才定位」，修复后同一提交新增 `docs/pitfalls/pitfall-NNN-*.md` 并更新索引（仓库制度，见 AGENTS.md「踩坑记录制度」）。
4. **实验现场保留**：事项「830：从参谋部到 AI推进」与「830实验·共识」目录**先不清理**——它们是给领导/灵基演示的现成材料，也是 ④期知识出口的对照样本。何时清理由 Guoxin Shan 决定。
5. **交回件**（转交回发起人）：§0 判定总表逐项结果 + gap §24.6 链接 + 看板最终态截图一张（队列头「上次巡检 · 发现 N 条」+ 时间旅程可见）。

### 9.2 若整体 PASS（判定 1–9 全过或仅有可解释偏差）

1. **结果同步 830 群**：把判定总表结论 + 看板截图发群（一条文本 + 一张图即可）——这是「AI推进真的在推进」的第一份对外证据，也兑现 16:51 群消息里预告的「跑通后出实验报告」。发送前把措辞给 Guoxin Shan 过目。
2. **触发 ③.2 开发**：通知 Guoxin Shan 实验通过，由他确认后再写 ③.2 开发计划（意图线程订阅落地，验收口径 spec §15.5）。实验中若发现「订阅缺失导致的别扭」（例如 scan 必须手写 groups、无法从事项反查渠道），记进计划的动机段。
3. **④期素材固化**：第 0 波手工做的「转录→四段纪要→入库」流程与耗时记录交回——它就是 ④期知识出口要自动化的基线（人工成本 vs 自动化收益的对照数据）。

### 9.3 若有 FAIL 项（按类别分流，不要笼统重跑）

| FAIL 类别 | 典型判定项 | 处置 |
|---|---|---|
| 判据/教学面问题（模型把噪音喂进来、该打扰没打扰） | 6、5 | 不改代码。把具体消息与模型判断记进 §24.6，提修订建议给 spec §13 判据文本 / `INSPECT_DISCIPLINE`，等拍板后改 spec 再改 `advance.ts` 教学字符串 |
| 机制 bug（去重不幂等、过滤失效、cursor 不前进、状态行不更新） | 7、8、4 | 修 `packages/tool-yzj` 对应逻辑 + 补单测 + pitfall 回写；重跑第 2 波（cursor 可再次回拨） |
| 门控异常（多弹卡/该弹不弹） | 9、3 | 对照 spec §13.5 门控表定位 `guard.ts` `WRITE_SPECS`；这是合同级缺陷，优先级最高，修复后重跑第 1 波③ |
| 环境/操作问题（GUI 没重启、模型连接错误、composer 误触） | 任意 | 不算产品 FAIL；按 §1 前置检查排除后重跑该波次，勿记入判据精度结论 |

FAIL 修复涉及代码时遵守仓库规矩：改 host 面 `pnpm run build`、改 browser half 另跑 `pnpm run bundle`、验收前重启 GUI、提交前全量 `pnpm test`；文档随代码同提交更新。

### 9.4 明确不属于本实验的后续（防止执行者顺手多做）

- 不要开始实现 ③.2（等 Guoxin Shan 确认计划）。
- 不要把纪要流程做成自动化脚本或工具（那是 ④期，先要人工基线）。
- 不要往迁移文档 §6 灵基需求清单里加新条目以外的改动（发现新需求可以**追加**，不重写既有条目）。
- 不要 git commit（证据与改动留工作区，由 Guoxin Shan 决定提交时机）。
